<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wakeupapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WakeUpApplication">
        <activity android:name=".vistas.VistaAlarmas"></activity>
        <activity android:name=".vistas.CreateGroup" />
        <activity android:name=".vistas.CreateAlarm" />
        <activity android:name=".vistas.Verify" />
        <activity android:name=".vistas.SMS" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.WakeUpApplication.NoActionBar" />
        <activity android:name=".vistas.RegisterActivity" />
        <activity android:name=".vistas.LoginActivity" />
        <activity android:name=".vistas.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".vistas.AlarmsActivity" />
    </application>

</manifest>